import {render} from '@testing-library/react';
import {PostCard} from '.'

describe('<PostCard />', () => {
    it('should render POstCard correcty', () => {

        render(<PostCard />);
    })

}) 